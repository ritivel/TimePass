org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# flutter_inappwebview_android 1.1.3 still selects the legacy default profile.
# Allow that transitive plugin while its upstream stable release catches up to
# AGP 9; Nakul's own release rules continue to use the optimized R8 defaults.
android.r8.proguardAndroidTxt.disallowed=false
